#Server port
server.port = 8080

# Enabling LiveReload when possible
spring.devtools.livereload.enabled=true

# Configuration for MySQL 8
spring.datasource.url = jdbc:mysql://localhost:3306/library?serverTimezone=UTC

# Database libraryUser
spring.datasource.username=root

# Database password
spring.datasource.password=toor

# Class to connect to the database
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Modify the schema if necessary
spring.jpa.hibernate.ddl-auto=update

#Spring Security

#spring.security.libraryUser.name = mateo
#spring.security.libraryUser.password = 123
